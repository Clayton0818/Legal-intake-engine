// Placeholder — c23's actual build replaces this with the real intake
// chat widget. Kept minimal here so `next build` succeeds before that
// work lands.
export default function ChatHome() {
  return (
    <div className="mx-auto max-w-lg p-6">
      <p className="text-sm text-slate-600">Intake chat widget lands here — see board card c23.</p>
    </div>
  );
}
