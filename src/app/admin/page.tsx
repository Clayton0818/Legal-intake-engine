// Placeholder — c24's actual build replaces this with the real matters
// queue / dashboard. Kept minimal here so `next build` succeeds before
// that work lands.
export default function AdminHome() {
  return (
    <div className="rounded-lg border border-slate-200 bg-white p-6">
      <h1 className="text-lg font-semibold text-slate-900">Staff Console</h1>
      <p className="mt-1 text-sm text-slate-600">Matter queue and detail views land here — see board card c24.</p>
    </div>
  );
}
